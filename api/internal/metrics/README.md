# Metrics

All application metrics are defined in [`metrics.go`](metrics.go) and pushed
directly to **Google Cloud Monitoring** via the
[OpenTelemetry GoogleCloud exporter](https://github.com/GoogleCloudPlatform/opentelemetry-operations-go).
No collector daemon is required.

## Architecture

```
Go binary  (systemd timer, exits after each run)
  └─▶  GoogleCloud OTel exporter  (in-process)
         └─▶  Google Cloud Monitoring API
```

Because the binary exits after every run, `MeterProvider.Shutdown()` is called
in `main.go` before exit to flush all pending data points.

Counters use **delta temporality** — each run reports its own increments.
Google Cloud Monitoring stitches deltas into cumulative time-series automatically.

## Configuration

| Environment variable           | Required | Description |
|-------------------------------|----------|-------------|
| `GOOGLE_APPLICATION_CREDENTIALS` | Yes | Path to `service-account.json` (generated by Terraform). The GCP project is detected automatically from the key file. |

The required IAM role is `roles/monitoring.metricWriter`, bound in
`infra/terraform/gcloud/main.tf`.

## Metric Catalog

All metric names are prefixed `lab.` and land under
`custom.googleapis.com/lab/` in Cloud Monitoring.

### Tier 1 — Business Outcomes

| Metric | Type | Unit | Labels | Description |
|--------|------|------|--------|-------------|
| `lab.run.duration` | Histogram | `s` | `status`: `success`/`failure` | Total wall-clock time of one orchestrator run |
| `lab.run.total` | Counter | — | `status`: `success`/`failure` | Number of completed runs |
| `lab.vm.restore.total` | Counter | — | `status`: `success`/`failure` | VM snapshot restores attempted |
| `lab.vm.restore.duration` | Histogram | `s` | — | Time to revert a single VM snapshot |
| `lab.calendar.events.active` | UpDownCounter | — | — | Active calendar events detected this run |
| `lab.email.send.total` | Counter | — | `status`: `success`/`failure`, `has_attachment`: `true`/`false` | Credential emails sent |
| `lab.password.rotation.total` | Counter | — | `status`: `success`/`failure` | ESXi user password rotations performed |

### Tier 2 — Operational Visibility

| Metric | Type | Unit | Labels | Description |
|--------|------|------|--------|-------------|
| `lab.wireguard.key.rotation.total` | Counter | — | `status`: `success`/`failure` | WireGuard key-pair rotations attempted |
| `lab.wireguard.peer.registration.total` | Counter | — | `status`: `success`/`failure` | OPNsense peer registrations attempted |
| `lab.calendar.fetch.duration` | Histogram | `s` | `status`: `success`/`failure` | Latency of the Google Calendar API call |

### Tier 3 — Inventory / Nice-to-Have

| Metric | Type | Unit | Labels | Description |
|--------|------|------|--------|-------------|
| `lab.vm.inventory.total` | UpDownCounter | — | — | VMs discovered in the ESXi inventory |

## Instrumentation Locations

| Metric | Source file | Function |
|--------|------------|----------|
| `lab.run.duration`, `lab.run.total` | `orchestrator.go` | `Run()` |
| `lab.vm.restore.total` | `orchestrator.go` | `RestoreVMs()` — after `RestoreVMsWithPasswordRotation` returns |
| `lab.calendar.events.active` | `orchestrator.go` | `FetchActiveEventsAt()` — after `FilterActiveEvents` |
| `lab.calendar.fetch.duration` | `orchestrator.go` | `FetchActiveEventsAt()` — wraps `ListEvents` |
| `lab.email.send.total` | `orchestrator.go` | `RestoreVMs()` — per email send call |
| `lab.password.rotation.total` | `orchestrator.go` | `RestoreVMs()` — after password map is populated |
| `lab.wireguard.key.rotation.total` | `orchestrator.go` | `RestoreVMs()` — per `RotateUserKey` call |
| `lab.wireguard.peer.registration.total` | `orchestrator.go` | `RestoreVMs()` — per `RegisterPeerWithOPNsense` call |
| `lab.vm.inventory.total` | `orchestrator.go` | `FetchVMInventory()` |

## Graceful Degradation

Metrics are **optional**. If `InitProvider` fails (e.g. wrong credentials,
no network access to Cloud Monitoring), the binary logs a warning and
continues without metrics. All instrumentation sites guard against a `nil`
`*Metrics` pointer.

## Running Without Metrics Locally

Simply do not set `GOOGLE_APPLICATION_CREDENTIALS`. The initialisation will
fail gracefully and the application will run in metrics-disabled mode.
