# ESXi Configuration
ESXI_URL=https://esxi.example.com
ESXI_USERNAME=root
ESXI_PASSWORD=your_password_here
ESXI_INSECURE=true

# SMTP Configuration (optional - for sending password emails)
# For Gmail: Enable 2FA and generate app password at https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com

# SMTP Password - choose ONE method:
# Method 1: Direct environment variable (for local development)
SMTP_PASSWORD=your-app-password-here

# Method 2: File-based secret (recommended for production)
# SMTP_PASSWORD_FILE=/run/secrets/smtp-password

SMTP_FROM=your-email@gmail.com

# Optional: Test mode - send all emails to this address instead of real recipients
# Useful for testing without spamming users
TEST_EMAIL_ONLY=your-test-email@gmail.com

# OPNsense WireGuard API Configuration
OPNSENSE_API_KEY=your-opnsense-api-key-here
OPNSENSE_API_SECRET=your-opnsense-api-secret-here
